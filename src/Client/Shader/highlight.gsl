vec4 effect(vec4 color, sampler2D texture, vec2 texture_coords, vec2 screen_coords){
	
	float rho = length(2*texture_coords-vec2(1,1));
	float alpha = smoothstep(0.45,1,rho);
	alpha = alpha == 1.0 ? 0: alpha;
	return vec4(
		43/255.0,
		153/255.0,
		196/255.0,
		alpha
	);
}